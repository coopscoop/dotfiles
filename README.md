# dotfiles

<leader><space>  Snacks find files
<leader>F        find lines
DELETE: move to `b` binds <leader>fb       buffers
DELETE: move to `s` binds <leader>fc       commands
DELETE: move to `s` binds <leader>fh       help
DELETE: move to `s` binds <leader>fk       keymaps

<leader>sr       recent files
DELETE: don't need it. fast enough to get to my dotfiles <leader>sn       config files

these are fine
<leader>bn       next buffer
<leader>bp       previous buffer
<leader>bd       delete buffer
<leader>bD       force delete buffer
<leader>ba       delete all buffers
<leader>bo       delete all other buffers
ADD: <leader>B   buffer search

-                Neo-tree
